spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lbjfish/springcloud-config
          search-paths:
          username:
          password:
      label: master